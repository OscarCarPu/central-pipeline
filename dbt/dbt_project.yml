name: central_pipeline
version: 1.0.0
config-version: 2

profile: central_pipeline

model-paths: ["models"]
macro-paths: ["macros"]
target-path: "target"

models:
  central_pipeline:
    staging:
      +schema: staging
      +materialized: view
    marts:
      +schema: marts
      +materialized: table
